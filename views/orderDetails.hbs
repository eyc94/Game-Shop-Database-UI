<div class="container-fluid">
    <nav class="fixed-top navbar navbar-expand-lg navbar-dark bg-secondary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Game Store Database UI</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-item nav-link" href="./customers">Customers</a>
                    <a class="nav-item nav-link" href="./products">Products</a>
                    <a class="nav-item nav-link" href="./orders">Orders</a>
                    <a class="nav-item nav-link" href="./reviews">Reviews</a>
                    <a class="nav-item nav-link" href="./orderDetails">Order Details</a>
                </div>
            </div>
        </div>
    </nav>

    <h1 class="text-center">Order Details</h1>

    <h3>Add Order Detail</h3>

    {{!-- Form to add order details --}}
    <form id="addOrderDetailForm" method="POST" action="/orderDetails/addOrderDetail">
        {{> orderDetailForm}}
    </form>

    <h3>Existing Order Details</h3>

    {{!-- Create a table --}}
    <table id="orderDetailsTable" class="table table-striped table-hover table-bordered">
        {{!-- Table header section --}}
        <thead>
            {{!-- First row has the key printed of each object as a header cell. --}}
            <tr>
                {{#each data.[0]}}
                <th>
                    {{@key}}
                </th>
                {{/each}}
            </tr>
        </thead>

        {{!-- Body section --}}
        <tbody>
            {{!-- Print the orderID, productID, quantity --}}
            {{#each data}}
            <tr>
                <td>{{this.orderID}}</td>
                <td>{{this.productID}}</td>
                <td>{{this.quantity}}</td>
                <td><a class="btn btn-sm btn-outline-success"
                        href="/orderDetails/editOrderDetail/{{this.orderID}}{{this.productID}}">Update</a></td>
                <td><a class="btn btn-sm btn-outline-danger"
                        href="/orderDetails/{{this.orderID}}{{this.productID}}">Delete</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>